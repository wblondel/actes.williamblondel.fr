#!/bin/sh

if [ "$#" -ne 1 ]; then
    echo "Usage: $0 <sqlite_file>"
    exit 1
fi

sqlite_file="$1"

if [ ! -f "$sqlite_file" ]; then
    echo "File not found: $sqlite_file"
    exit 1
fi

sqlite3 "$sqlite_file" <<EOF
SELECT * FROM your_table;
-- Perform other SQLite operations here
.quit
EOF